$(document).ready(() =>
{
    $('.datepicker').datepicker(
        {
            format: 'yyyy',
            viewMode: 'years',
            minViewMode: 'years'
        }
    );//Turn input into datepicker*/
});